# 7.15 combined fork product candidate

Status: full-diff audit complete, 0 findings open; ready for the Copilot /
multi-agent review phase. Branch `audit/715-scope-repair`, PR
[#756](https://github.com/BitHighlander/keepkey-firmware/pull/756), unmerged into
fork develop. This document is not a claim of perfect firmware.

## Source and scope

Frozen starting source: `a56fb3e88d7dbbe31a321179c10a8fd2023d8f0c`. Candidate branch: `audit/715-scope-repair`. Retain source ancestry and apply small audit
units; do not merge into develop. The audit-unit receipts in `audit-units/` record
individual contracts, reproduced failures, fixes, and focused checks.

Preserve the frozen canonical 7.15 feature set: full and Bitcoin-only variants;
runtime provider context and icons; EVM raw/decoded disclosure; Solana signing
and transaction-bound LUT support; supported Cosmos-family and other chain
engines; Zcash Orchard and the Ironwood handlers present in this source; RNG
health, dice, BIP-85, setup ceremonies, and storage migration. The older RC18 host
skips do not describe this canonical source. Keep provider context additive and
RAM-scoped, structured EIP-712 disabled by its existing policy, and persistent
provider identities retired. No pinned production provider keys or alpha worktree
changes are imported.

## Dependency pins

- `code-signing-keys`: `a6470bd8598e5e9a7bfc38bf139a5e5a616f05ec`
- `deps/crypto/trezor-firmware`: `8a392f70a5d5575ece3dfb35f115d4a4b27f497c`
- `deps/device-protocol`: `8545cd5b615f5832374afbf06387a3f28869285e`
- `deps/googletest`: `7888184f28509dba839e3683409443e0b5bb8948`
- `deps/python-keepkey`: `6a85668736d7d4ecbb5dddf9478be65074a31154`
- `deps/qrenc/QR-Code-generator`: `6dfbfdad5d9303ed190d1c3cb7bec34b565b6ce8`
- `deps/sca-hardening/SecAESSTM32`: `71d356a1141624994cf613bd2d2583892e8e6d5a`

Every `.gitmodules` branch key names a branch verified to contain the pin
above, so `git submodule update --remote` cannot rewind a release pin. Two of
those branches are not yet canonical:

- `deps/python-keepkey` tracks `fix/hive-wire-asset-symbols`
  (keepkey/python-keepkey#223), which carries the Hive wire-symbol test
  correction this firmware fix requires. **Re-pin to
  `reconcile/upstream-sync` once #223 merges.**
- `deps/device-protocol` tracks `up/release-protocol`, which is canonical for
  the release protocol; `master` does not carry it.

## Acceptance gates

- Complete native suites, including restored FSM tests and applicable crypto.
- Full host integration and storage power-cycle tests against this candidate.
- Exact candidate CI: formatting, static analysis, secret/submodule checks,
  ARM build and SRAM floor, unit/integration variants, evidence aggregate gate.
- Resolve every known applicable finding. Record source-policy conflicts and
  exclusions explicitly; do not replay superseded behavior or weaken tests.
- Final physical OLED and signed-upgrade checks remain release-signing gates.
  Emulator display artifacts do not substitute for a physical device.

Only advance the canonical branch after candidate CI and applicable internal
checks pass. Record exact code head, workflow run, artifact provenance and skips
before declaring readiness for the final Copilot phase. Do not request Copilot
inside this internal rehearsal or automatically publish emulator artifacts.

## Accepted internal receipt

Validated code head: `ad68841a5e20fb1683da1697b851ed859ef3b845`.
Validated tree: `40e4611c33fb5009fe827b2c6a28f47e9355be30`.
[CI run 34709140171](https://github.com/BitHighlander/keepkey-firmware/actions/runs/34709140171)
passed every required job on that exact head: formatting, static analysis,
secret and submodule checks, ARM builds with the runtime-SRAM link gate,
emulator builds, native unit suites, host integration, the shared-library
suite, generated-report validation and the CI gate. `publish-emulator` is
workflow_dispatch-only and is skipped, as designed.

Host results on that run: 1342 cases, 1305 passed, 37 skipped, 0 failed, 1167 OLED
frames captured. Pinned python-keepkey for that run: `6a85668736d7d4ecbb5dddf9478be65074a31154`.

A skip is not a pass, so the skips are itemised rather than summed. The
largest reasons on this run:

- 11: Bitcoin-only firmware required to run this test
- 5: no kkemu binary to start and stop
- 4: dylib screenshot regression
- 4: Firmware does not implement structured EIP-712 (EthereumSignTypedData is not handled)
- 2: dylib confirm-flow regression
- 2: power cycle needs an emulator process this harness owns

The evidence bundle (PDF, merged JUnit, manifest, checksum) is the
`test-report` artifact of that run and binds this firmware SHA and this
python-keepkey SHA.

This receipt is documentation only: the validated head named above is this
commit's parent, and the commit carrying these words changes no code, no test
and no pin. A reader checking the claim should diff this commit to see that.

## Included audit heads

Each row is an accepted head in the canonical candidate's ancestry. Audit PRs
retain their frozen predecessor bases for bounded review.

| Audit unit | Head | Fork PR |
| --- | --- | --- |
| Decode boundaries | `67e9dba64` | #644 |
| Storage capacities | `5e6118af1` | #647 |
| Passphrase transition | `fcb4a20ca` | #648 |
| Cipher cleanup | `92408fde9` | #652 |
| EOS authorization | `09f535001` | #654 |
| Provider icon placement | `a864b4fd3` | #655 |
| Low-level revocation | `da5066be6` | #656 |
| Authenticator source cleanup | `f5e00811a` | #658 |
| Generated token dependencies | `163652a88` | #659 |
| Shared test board bootstrap | `28cf2ae83` | #662 |
| Zcash export authorization | `d4c82dd33` | #663 |
| Duplicate-detector evidence | `2ddd36f4d` | #664 |
| Complete capability validation | `aa884ebe6` | #665 |
| Revocation condition cleanup | `c8dd8717b` | #666 |
| Debug backup acknowledgement | `af6c0bfcf` | #667 |

## Scope closure and promotion

The audit-unit receipts close transport decoding, storage capacities and cipher
cleanup, passphrase transitions, EOS authorization, provider icon placement,
low-level revocation, authenticator source erasure, token build dependencies,
shared native fixture initialization, Zcash viewing-key consent and account
bounds, D-01 positive duplicate detection, and actual product capability gates.
The integration cleanups consolidate adjacent PIN conditions and require each
debug backup subpage to consume its own acknowledgement. The latter has a
deterministic failure-before/pass-after queue-consumption regression; it closes
the recovery response misalignment exposed by CI 34281427504. Focused cppcheck
and complete native/host suites pass.

The alternate 7.15.0 source is reconciled in
`audit-units/alternate-7150-reconciliation.md`. Its unknown-version lockout is
superseded by the current documented downgrade-erasure policy; it is not an
unresolved missing fix. Required test files are registered and stale RC18 skip
gates have been removed for capabilities implemented in this product.

This receipt adds documentation only relative to the validated code head.
Prove the non-documentation diff empty and check the live canonical ref before
advancing `release/7.15` normally from
`a56fb3e88d7dbbe31a321179c10a8fd2023d8f0c`. Keep product PR #629 into fork develop
open and unmerged. This receipt supersedes pre-assembly status snapshots.

No known unresolved in-scope finding remains under the recorded contract.
The internal candidate is frozen for the later upstream-shaped Copilot phase;
Copilot has not been requested for this rehearsal. Physical OLED, signed-upgrade
verification, external provider-tooling test-key disposition and upstream public
dependency reconciliation remain their explicitly separate release/upstream gates.
No release signing, publication, flashing or develop merge is claimed.

## Full-diff audit, 2026-09-12

This candidate was audited by reviewing every changed file against its base,
not by re-reading the summaries above. 249 findings were raised across the
three release lines; the 104 rated P1/P2 were each re-checked by three
independent reviewers along separate lines of attack (is it reachable, does it
matter on a shipped release, is it already covered), and 84 survived that.
Everything that survived and applies to this line is fixed at the head below.

Each fix carries a test, and where a control was possible the unfixed code was
built and the test confirmed to fail against it -- a test that passes either
way is recorded as such in the file rather than counted as coverage.

Findings on this line are listed with their resolution in `audit-units/`.
The load-bearing ones for a reader of this receipt:

- cipher recovery committed the EMPTY mnemonic when a ceremony was driven with separators only: the count gate passed with zero words and the wire-default enforce_wordlist skipped the checksum, so the device stored a publicly derivable seed and reported success
- BIP-85 child seeds were drawn with the unpaged constant-power renderer, which drops every character after the first that will not fit -- pages are packed at 225 px and drawn at 124 px, and the widest page this packer emits does not fit (measured)
- the MAYAChain send screen scaled every host-chosen denom by CACAO's 1e10, so 1 'maya' displayed as the value of 1,000,000
- consent screens bypassed the measured renderer, the >99-page cap truncated instead of refusing, and a body vsnprintf() had already cut could still be approved -- all three reverted into this candidate by a56fb3e88 while keeping the comments that describe the opposite
- the THORChain memo's ABI tail padding was signed unchecked (up to 31 attacker-chosen bytes) and the deposit decoder was not pinned to a router, so deposit-shaped calldata to any contract inherited the clear-sign UX and skipped the AdvancedMode gate
- Tendermint envelopes (both chains) armed before validation, accepted messages past their declared budget, and MAYAChain never emitted the msgs[] comma separator -- a two-message document hashed as invalid JSON
- multisig quorum was validated on outputs but not inputs, where m feeds the weight and the excessive-fee threshold
- Hive signed the display asset symbol ('HIVE') where hived writes 'STEEM'; the device's own golden vectors, captured from hived, said so, while the integration test asserted the firmware's mistake
- auto-lock measured wall-clock rather than idleness and tore down live ceremonies; the lock's own abort redrew the home screen, so the screensaver vanished one tick after locking
- two release gates exited 0 having read nothing, reporting '0 files checked' as a pass
- the Hive account-name nanopb bounds were a byte short of the protocol's 16 characters (max_size counts the NUL), so a legal account was refused inside pb_decode, before any handler could say why

A second pass then re-read the P3s that the first pass had triaged rather than
re-checked, because "read and judged not to warrant a change" is a weaker
claim than the rest of this receipt makes. Ten were real and are now fixed:

- a derived private key was left in the shared `fsm_derived_node` scratch on
  two error exits a host can reach on demand, by sending an ERC-20 transfer
  whose encoded recipient does not match the derived path
- an EIP-712 `int` of "-0" sign-extended on the '-' CHARACTER rather than the
  parsed value, so the screen read "-0" while the hash committed to -2^64
- Orchard actions were validated on three of the five fields the commitment is
  computed over, so a host could omit `nullifier` and have the device verify a
  commitment over an implicit rho of zero; a non-canonical rho was masked
  rather than refused
- P2SH was accepted as a transparent INPUT, then signed with a P2PKH key and
  the wrong scriptCode -- an address shown as spendable, and a signature no
  node accepts
- both sighash-suffix writes in signing.c relied on a cap their own comments
  claimed to have made unnecessary; the single-signature branch never received
  7.14.2's half of that hunk at all
- the *_without_button_request() entry points emitted a ButtonRequest per page,
  telling a host about presses it never asked to arbitrate
- the RNG health gate read the hardware fault latch only before a draw
- the transfer amount screen took the Wanchain ticker from a static the
  transfer path had not set

The rest were verified at head -- some already fixed by another finding's
change, some unreachable on this build -- and `audit-units/` records which, so
the triaged list is not mistaken for a list of live defects.

Two defects were found in the audit's own work and corrected the same way: a
router pin that refused Maya deposits (CI caught it), and a `const` violation
that only ARM GCC rejects (the emulator's clang accepts it). Local ARM
cross-builds using CI's own base image now cover every variant.
